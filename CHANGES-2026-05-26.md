# 修正事項まとめ（2026-05-24 〜 2026-05-26）

最終更新: 2026-05-26 22:30 JST

本ファイルは過去2日間に行った主な修正をまとめたものです。コミット単位の詳細は `git log` を参照してください。

---

## 1. 発注管理（Orders）

### 1-1. 発注作成時の日付編集対応
- 新規発注モーダル（手動・一括）で「発注日」「希望納期」を編集可能に
- API（POST /api/orders）が `orderDate` を受け取れるよう拡張
- コミット: `78a5617`

### 1-2. 発注詳細から直接入庫処理ができるように統合
- 発注詳細モーダル内に「今回の入庫処理」ブロックを追加
- 残あり明細をカード形式で表示し、`今回入庫数 / 不足数 / 理由 / 納期見込み` を一括入力
- 0 を placeholder 化、不足数 0 のときは理由・納期見込みを disabled に
- コミット: `43f981c`, `11a3c16`

### 1-3. 不足/不良の階層記録
- 新規テーブル `TOrderDetailShortage`（残数・理由・状態・納期見込み）
- 発注一覧ページ詳細の明細配下に履歴を階層表示
- 編集（数量・理由・納期見込み）・削除（編集ペンシル + ゴミ箱）対応
- 「解決」ボタンと「状態」列は冗長と判断し削除（後日入庫で `receivedQty` が自動更新されるため）
- 「全量欠品」ボタンは新方式に一本化して削除（不足数=残数で同等の結果）
- コミット: `cd7c83a`, `e259d57`, `5f45c5b`, `86b48ee`

### 1-4. 残数マイナス防止
- 入庫API `/api/receives` で `qty > 残数` のリクエストを 400 で拒否
- 既存DBで発生していた `received_qty > qty` の不整合データを 1 件補正
- コミット: `2d50f76`

### 1-5. 振替発注（メーカー欠品時の別仕入先発注）
- ユーザー側で実装。メーカー欠品マーク後に振替先仕入先を選んでドラフト発注作成
- 元明細と振替先明細が双方向リンク（`replacesDetailId`）
- 表示理由を「欠品・不良・破損」に限定
- コミット: `82d7b9b`, `c637569`

---

## 2. 入庫処理（Receives）

### 2-1. 検査プルダウン拡張
- `合格 / 条件付合格 / 不合格` → `合格 / 不良 / 破損 / 欠品 / その他` に再編
- `rejectReason` には選択値そのものを保存
- コミット: `43f981c`

### 2-2. 入庫処理画面の刷新
- 旧: シングルアイテム選択UI → 新: 検索付き一覧 + 詳細パネルに統一
- 発注管理と同じ操作感に
- コミット: `7482db8`, `e573952`

---

## 3. 製造管理（Production）

### 3-1. 4ビュータブ化
- 既存の一覧表示 → タブ切替式に：**一覧 / カンバン / ガントチャート / 工程マスタ**
- 一覧: 既存テーブル + ステータスフィルタを動的化
- カンバン: 工程マスタから列を動的生成、ドラッグ&ドロップ + ボタンでステータス変更、カードに進捗バー
- ガント: 開始日〜納期のバー表示
- コミット: `55fcd63`

### 3-2. 工程マスタのカスタマイズ
- 新規テーブル `MProductionStage`（key / name / color / sortOrder）
- 新規テーブル `MProductionTask`（各工程のチェックリスト）
- 工程の追加・編集・削除・並び替え・色変更が UI 上で可能
- デフォルト工程を **「受注 → 設計 → 板金 → 塗装 → 組み立て → 納品 / 遅延」** に刷新
- 既存指図のステータスも新キーに自動マップ
- コミット: `55fcd63`, `2b33d8c`

### 3-3. 指図ごとの工程スケジュール
- 新規テーブル `TProdOrderStage`（指図 × 工程 × 開始日・納期・状態）
- 各製造指図のアクティブ工程を詳細表示時に自動初期化
- 工程ごとの日程入力 + 編集 + ガントへの反映
- コミット: `207aad6`

### 3-4. 製造指図の詳細ページ
- ポップアップ（Modal）→ フルページ画面遷移に変更（「一覧へ戻る」ボタン付き）
- **管理者ビュー**: 工程ごとの日付入力付きミニガント + 全工程のタスクボード
- **作業者ビュー**: 現在工程の大きなチェックボックス + 他工程の進捗バー
- カードクリック・一覧の「詳細」ボタンから遷移
- コミット: `207aad6`, `0d9ede6`, `39b42b6`

### 3-5. グローバルガントの全幅化 + ドラッグ操作
- 旧 px 固定 dayWidth → CSS grid `240px + 1fr` + % ベースに変更（常にページ幅にフィット）
- 集約行を「全体バー1本」→ **全工程バーを同一行に横並び** に変更（提示画面と同等）
- バー本体ドラッグで期間スライド、両端ハンドルで開始/終了のみ変更、`mouseup` で自動保存
- 単純クリックは詳細ページ遷移を維持
- コミット: `02a2855`, `2b33d8c`

### 3-6. ガント日付の粒度切替（日 / 週 / 月）
- 長期表示で日付ラベルが重なる問題を解消
- 日: `M/D` / 週: `M月 + W番号` / 月: `YYYY + M月`
- 期間切替で粒度も自動最適化（2週・1ヶ月=日、3ヶ月=週、6ヶ月・1年=月）
- バーは引き続き % 計算で位置正確
- コミット: `43a4b30`

### 3-7. 受注ステータスと製造ステータスの分離
- `TProdOrder` に `salesStatus` カラム追加
- 受注ステータス: `見積 / 受注確定 / 出荷済 / キャンセル`
- 製造ステータス: 工程マスタの key（受注 / 設計 / 板金 / …）
- コミット: `2b33d8c`

---

## 4. 全体UI / その他

### 4-1. フォントサイズ一括引き上げ
- `text-[10px]` / `text-[11px]` / `text-xs (12px)` → 一段階上に統一（本文は基本 `text-sm` 14px）
- 見出し `font-bold text-sm` → `font-bold text-base`
- `shared.tsx` の Btn / StatusBadge / Field / Card / inputClass も同様
- コミット: `277cfa5`

### 4-2. ガント表示期間追加
- 既存の 2週 / 1ヶ月 / 3ヶ月 に加えて **6ヶ月 / 1年** を追加
- デフォルトを 3ヶ月に
- コミット: `277cfa5`

### 4-3. 製品管理メニュー追加（UIプレースホルダ）
- サイドメニューに「製品管理」項目を追加
- 中身は箱のみのプレースホルダ
- コミット: `82a9346`

### 4-4. dev サーバポート変更
- `next dev` のポートを **3100** に固定（`npm run dev`）
- コミット: `8532d73`

### 4-5. ヒアリング事項の整理
- 受注ステータス・工程フロー・権限・通知・マルチテナント等のクライアント確認事項を `HEARING.md` にまとめ
- コミット: `2b33d8c`

---

## 5. 注意事項 / 既知の制約

- **工程マスタ刷新時の影響**: 旧キー(`planned/allocated/picking/completed`) の指図は新キー(`order_received/design/assembly/delivery`) に自動マップ済み
- **遅延ステータス**: 現状は順序最後（sortOrder=99）の1工程として登録。将来「特殊フラグ」扱いにするかは要検討（HEARING.md 参照）
- **受注ステータスのデフォルト**: 新規登録時は `quote(見積)`、既存データは `confirmed(受注確定)` でバックフィル
- **工程内タスクの進捗率**: バー上に N/M 表示は未実装。実装可能（HEARING.md 参照）

---

## 6. 関連ドキュメント

- `HEARING.md`: クライアント確認事項（決まり次第本実装へ反映）
